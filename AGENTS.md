# HX-ProxyGroup Agent Development Guide

本文件约束所有 AI / Agent 对 HX-ProxyGroup 的后续修改。开始编码前必须先阅读：

1. `README.md`
2. `docs/V1_CORE.md`
3. `docs/ARCHITECTURE.md`
4. `docs/RELIABILITY.md`

## 1. 产品边界

- 本项目是代理**控制面**，不是新的代理协议内核。
- v1 数据面固定为 Mihomo；不得自行实现 VMess、VLESS、Trojan、Hysteria、TUIC、SOCKS5 或 HTTP CONNECT 数据转发。
- 控制面不得进入实际代理流量转发路径。
- 浏览器 SSH 终端属于 v2，不得挤入 v1 核心链路。
- “支持全部协议”必须表述为数据面当前版本支持的协议集合，不能伪造协议兼容性。

## 2. 固定技术栈

### 后端

- Go。
- 标准库优先，依赖必须说明必要性。
- SQLite + WAL；v1 不引入 MySQL、Redis、Kafka、etcd 或独立任务队列。
- REST 管理 API + SSE；只有真正双向场景才使用 WebSocket。
- 日志采用结构化日志，默认输出给 journald。

### 数据面

- Mihomo 独立受管进程。
- 所有数据面交互通过 `internal/dataplane` 接口。
- 业务领域模型不得直接依赖 Mihomo YAML 类型。

### 前端

- React + TypeScript。
- Tailwind CSS + shadcn/ui。
- 图标使用 Lucide。
- 生产构建为静态资源，不运行 Node.js Server。
- 状态管理区分 Server State 和本地 UI State；禁止把所有状态塞进单个全局 Store。

## 3. 模块边界

必须保持：

```text
api -> application service -> domain/interface -> repository/dataplane
```

禁止：

- Handler 直接写 SQL。
- Repository 调用 HTTP API。
- 前端直接访问 Mihomo External Controller。
- Probe 直接修改代理组或 Listener。
- 数据库实体直接作为公开 API DTO。
- 在多个模块重复解析同一种订阅格式。
- 通过字符串拼接生成 YAML / JSON 配置。

任何跨层捷径都必须先修改架构文档并说明原因。

## 4. 代码规模与可维护性

- 单个源文件目标不超过 500 行；自动生成文件、迁移和纯数据表可例外。
- 一个文件只承担一个清晰职责。
- 不创建 `utils` 大杂烩；按领域放置辅助逻辑。
- 不创建只有转发作用、没有抽象价值的接口。
- 不为“未来可能需要”提前构造复杂插件系统。
- 扩展点使用小接口和显式注册表，不使用反射驱动魔法。
- 避免前后端为单一页面共同维护一套高度定制、不可复用的状态协议。
- API 字段具有稳定语义；前端不得依赖错误消息字符串判断逻辑。

## 5. 并发规则

- 所有 goroutine 必须具有明确所有者、退出条件和取消路径。
- 不允许为每个节点创建永久 ticker / goroutine。
- 后台任务统一进入有界队列和 worker pool。
- 网络、数据库和数据面调用必须接收 `context.Context`。
- 禁止无界重试、无界 channel 和无界内存队列。
- 同一订阅刷新、同一节点同类 Probe 和配置 Apply 必须去重或串行化。
- 服务停止时按顺序停止接收任务、取消任务、Flush、关闭资源。

## 6. 配置与事务

- 数据库 Desired State 是控制面配置事实来源。
- 每次变更生成完整候选配置。
- 必须先校验候选配置，再原子发布。
- 数据面应用失败必须保留或回滚到上一版。
- 订阅刷新失败不得清空上一版快照。
- 网络请求不得发生在 SQLite 写事务内部。
- 配置输出必须稳定排序，确保同一输入得到稳定 diff。

## 7. 安全规则

- 不提交真实订阅 URL、Access Token、节点密码、私钥、SMTP 密码或数据库文件。
- 日志不得包含 Authorization、Cookie、订阅 Token、节点密码或完整订阅 URL。
- 管理 API 和 Mihomo External Controller 默认只监听环回地址。
- 新 Listener 默认只监听环回地址，公网暴露必须显式配置。
- 所有外部输入必须有体积限制、超时、格式验证和错误分类。
- 订阅获取和探测功能必须考虑 SSRF、DNS Rebinding 和重定向目标变化。
- 密码使用现代密码哈希；需要恢复的秘密使用 AEAD 加密存储。
- WebSocket Path 不是认证机制。

## 8. 性能规则

- 控制面不得代理或复制用户流量。
- 生产进程模型保持一个控制面和一个数据面。
- 统计先在内存聚合，再批量写 SQLite。
- 禁止逐数据包、逐字节或默认逐连接写数据库。
- 列表和图表 API 必须限制时间范围、分页大小和返回点数。
- 订阅解析、节点评估和配置编译应支持基准测试。
- 性能声明必须附测试环境、命令和结果；没有数据时不得编造 QPS、RSS 或延迟。
- 新依赖需要检查常驻内存、线程/goroutine、CGO 和交叉编译影响。

## 9. 测试金字塔

### 单元测试

必须覆盖纯领域逻辑：

- 节点指纹与去重。
- 规则流水线。
- 评分和指标过期。
- 代理组依赖图。
- 配置编译稳定性。
- 告警状态机。
- 统计聚合。

### 集成测试

必须覆盖真实边界：

- SQLite 迁移、事务、备份恢复。
- Mihomo 配置校验、Apply 和 Rollback。
- HTTP / SSE API。
- SMTP 测试服务。

### 端到端测试

核心用户链路：

```text
添加订阅 -> 刷新 -> 检测 -> 建组 -> 创建 Listener -> 真实代理请求 -> 查看统计
```

前端关键功能需要浏览器自动化验证。UI 变更除测试外应保存运行截图用于人工确认。

## 10. 修改完成标准

每次实现一个功能至少完成：

- [ ] 代码能够格式化、编译和运行。
- [ ] 新增或更新对应单元测试。
- [ ] 涉及边界时增加集成测试。
- [ ] 涉及用户核心链路时增加端到端测试。
- [ ] 错误路径经过验证，不只测试成功路径。
- [ ] 没有新增秘密泄露或公网默认暴露。
- [ ] 没有无界并发、无界重试或永久任务泄漏。
- [ ] API / 数据库 / 配置变更同步更新文档和迁移。
- [ ] 只有验证通过后才能勾选 `docs/V1_CORE.md` 中的任务。

## 11. 变更流程

1. 明确本次修改对应的 v1 清单项。
2. 先写或更新领域模型、接口和验收测试。
3. 实现最小纵向闭环，不一次铺开多个未完成模块。
4. 运行格式化、静态检查、单元测试、集成测试和必要的 E2E。
5. 检查 Git diff，确认无凭据、大文件、生成缓存和调试代码。
6. 更新文档与清单状态。

禁止只生成大量骨架文件、TODO 和不可运行占位实现来宣称功能完成。

## 12. 本地进程生命周期

- 默认不得在后台启动前端开发服务器、控制面或 Mihomo 数据面。
- 只有在功能验证确有需要时才可临时启动相关服务，并应使用可追踪、可终止的进程会话。
- 任务结束前必须停止本次任务启动的全部服务和子进程，并确认没有遗留后台进程。
- 仅执行会自行退出的构建、格式化、静态检查和测试命令不视为启动后台服务。

## 13. 参考项目

`ref/` 下项目只用于研究：

- 默认不修改参考仓库。
- 不复制不兼容许可证代码。
- 借鉴设计时在文档或提交说明中记录来源和差异。
- HX-ProxyGroup 的领域模型和 API 不应直接照搬参考项目的内部实现。
